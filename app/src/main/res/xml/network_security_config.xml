<?xml version="1.0" encoding="utf-8"?>
<!-- REQUIRED (not prototype scaffolding): OfflineMapManager runs a short-lived
     LocalStyleServer on 127.0.0.1 to serve the map style during a region download,
     because MapLibre's offline downloader only accepts an http(s) style URL (it
     rejects asset://, data: and file://). Scoped to localhost only — the rest of
     the app stays cleartext-blocked. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
